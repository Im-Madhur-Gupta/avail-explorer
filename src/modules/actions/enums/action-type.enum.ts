/**
 * Types of blockchain actions that can be tracked
 */
export enum ActionType {
  /** Data submission to the blockchain */
  SUBMIT_DATA = "submitData",
  /** Token transfer between accounts */
  TRANSFER = "transfer",
}
